/**
 * lib/agent/intent.ts — deleted in Phase 5 (§2 of
 * Docs/planning/phase_5_agent_loop.md: "Phase 3 labelled it throwaway; the
 * planner and step resolver replace it"). This file is intentionally kept
 * as an empty placeholder rather than removed outright, so a stray import
 * of the deleted module fails loudly at compile time instead of a missing
 * spec file silently shrinking `npm run test:unit`'s count.
 */
import { describe, it } from 'vitest';

describe('intent (removed)', () => {
  it.todo('lib/agent/intent.ts was deleted in Phase 5 — see lib/agent/planner.ts and lib/agent/step-resolver.ts');
});
