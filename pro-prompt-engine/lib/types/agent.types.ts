/**
 * Agent types — stub.
 *
 * [Phase 3] fills this in with the gate, tier and actuation types. Created
 * now so the file tree matches architecture.md §3.6 from the first commit.
 */

/** [Phase 3: replaced by the real agent type set] */
export type AgentPhase3Stub = never;
