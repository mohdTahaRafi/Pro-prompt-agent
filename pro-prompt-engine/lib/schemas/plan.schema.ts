/**
 * Plan schema — stub.
 *
 * [Phase 4] fills this in with the planner's output shape and its Zod
 * schema. Phase 1 creates only the type reference RunRecord.plan needs.
 */

/** [Phase 4: replaced by the real plan shape — steps, verbs, expectations] */
export interface Plan {
  readonly __phase4Stub: true;
}
